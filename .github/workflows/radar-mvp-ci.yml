name: Radar MVP CI

on:
  pull_request:
    paths:
      - "radar-mvp/**"
      - ".github/workflows/radar-mvp-ci.yml"
  push:
    branches:
      - main
    paths:
      - "radar-mvp/**"
      - ".github/workflows/radar-mvp-ci.yml"
  schedule:
    - cron: "17 12 * * *"
  workflow_dispatch:

concurrency:
  group: radar-mvp-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality:
    name: TypeScript y pruebas
    if: github.event_name != 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: radar-mvp
    env:
      SUPABASE_URL: https://ci-placeholder.supabase.co
      SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-service-role-key-000000
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v6

      - name: Preparar Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: radar-mvp/package-lock.json

      - name: Instalar dependencias reproducibles
        run: npm ci

      - name: Validar TypeScript
        run: npm run typecheck

      - name: Ejecutar pruebas unitarias e integración
        run: npm test

      - name: Auditar dependencias de producción
        run: npm audit --omit=dev --audit-level=high

  production-smoke:
    name: Smoke de producción
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main'
    permissions:
      contents: read
      issues: write
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: radar-mvp
    env:
      E2E_BASE_URL: https://joinsclee-radar.juno8i.easypanel.host
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v6

      - name: Preparar Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: radar-mvp/package-lock.json

      - name: Instalar dependencias reproducibles
        run: npm ci

      - name: Medir salud y latencia de producción
        run: npm run monitor:production

      - name: Conservar diagnóstico del monitor
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: radar-production-monitor-${{ github.run_id }}
          path: radar-mvp/artifacts/production-monitor.json
          if-no-files-found: warn
          retention-days: 14

      - name: Instalar Chromium
        run: npx playwright install --with-deps chromium

      - name: Validar recorridos críticos en producción
        run: npm run test:e2e

      - name: Resumir fallo operativo
        if: failure()
        run: |
          echo "## Radar CRECE: smoke de producción falló" >> "$GITHUB_STEP_SUMMARY"
          echo "Revisar EasyPanel, Supabase y el runbook en radar-mvp/docs/PRODUCTION_RUNBOOK.md." >> "$GITHUB_STEP_SUMMARY"

      # Se abre incidente en cualquier ejecución del smoke, incluida la de push a
      # main: el fallo del 2026-07-27 justo tras el merge del PR #15 no abrió
      # ninguno porque este paso filtraba por evento, que es cuando más falta hace.
      - name: Abrir o actualizar incidente
        if: failure()
        uses: actions/github-script@v8
        with:
          script: |
            const title = '[Radar CRECE] Monitor de producción fallando';
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            // El evento importa para leer el incidente: en `push` el despliegue en
            // EasyPanel todavía no ocurrió, así que lo medido es el build anterior.
            const body = [
              'El smoke automático detectó una degradación de producción.',
              '',
              `Ejecución: ${runUrl}`,
              `Disparador: \`${context.eventName}\` · commit del repositorio: \`${context.sha.slice(0, 7)}\``,
              context.eventName === 'push'
                ? 'Aviso: en un push el despliegue todavía es manual, así que el smoke midió la revisión anterior, no este commit.'
                : 'Lo medido es la revisión que hay desplegada en este momento.',
              'Runbook: `radar-mvp/docs/PRODUCTION_RUNBOOK.md`',
              '',
              'Revisar primero `/health`, `/ready`, EasyPanel y Supabase.',
            ].join('\n');
            const { data: issues } = await github.rest.issues.listForRepo({
              ...context.repo,
              state: 'open',
              per_page: 100,
            });
            const incident = issues.find(issue => !issue.pull_request && issue.title === title);
            if (incident) {
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number: incident.number,
                body,
              });
            } else {
              await github.rest.issues.create({
                ...context.repo,
                title,
                body,
              });
            }

      # Asimetría deliberada: abrir es ungated, cerrar no. El despliegue en
      # EasyPanel es manual (runbook §3), así que el smoke de un push mide todavía
      # el build anterior: un verde ahí no acredita que el incidente esté resuelto.
      - name: Cerrar incidente recuperado
        if: success() && (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch')
        uses: actions/github-script@v8
        with:
          script: |
            const title = '[Radar CRECE] Monitor de producción fallando';
            const { data: issues } = await github.rest.issues.listForRepo({
              ...context.repo,
              state: 'open',
              per_page: 100,
            });
            const incident = issues.find(issue => !issue.pull_request && issue.title === title);
            if (incident) {
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number: incident.number,
                body: `Producción volvió a estar sana en la ejecución ${context.runId}. Cierre automático.`,
              });
              await github.rest.issues.update({
                ...context.repo,
                issue_number: incident.number,
                state: 'closed',
              });
            }
